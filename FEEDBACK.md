# 反馈：让你的 agent 写一份使用报告

给用过本插件的人。**在用完插件的同一个会话里**，把下面分割线以下的内容整段贴给你的
agent（或者让它读这个文件照做）。它会生成一份报告；你把报告贴到
<https://github.com/lzcm729/game-toolkit/issues/new>，标题加 `[feedback]` 前缀。

Claude Code、Codex 或别的 harness 都可以。每次有实质使用后跑一次，比攒一个月再总结有用。

---

用中文回答。

你刚才在这个会话里用了 game-toolkit 插件（或它的一部分）。现在给插件作者写一份使用报告。
报告只收**事实**：每一条都要能指到文件路径、指令原文或报错原文。作者拿它来改指引，
所以「哪里卡住、哪里靠猜、哪里执行不了」比「整体好用」有价值得多。

## 规则

- **观察还是推测**：每条标 `[观察]` 或 `[推测]`。`[观察]` 必须带证据：文件路径、
  逐字引用的指令原文、报错原文、命令和退出码。
- **引用原文**：提到某条指令时，逐字引用 SKILL.md 或 agent 文件里那一句，附路径。
- **空节写「无」**：模板的每一节都要出现。确实没有内容的节写一个「无」。
- **证据来源**：本会话上下文、你读过的插件文件、工作目录的 git diff 和新生成的文件、
  你的 harness 的会话日志（读得到的话）。

## 步骤

1. 盘点本会话读过的插件文件，列出路径。后面所有引用都从这个范围里来。
2. 按模板逐节填写。完成标准：每一节都出现，每条 `[观察]` 都带证据。
3. 写到当前工作目录的 `game-toolkit-feedback.md`（贴完可删），并把全文放进你的最终回答。
   日期用系统时间（`date`）。到这里你的工作就完成了：把报告贴到 issue 是用户的事。

## 模板

```markdown
# game-toolkit 使用报告

- 日期：
- 插件版本：（`.claude-plugin/plugin.json` 里的 version；找不到写「未知」）
- harness 与模型：（如 Codex CLI 0.x / gpt-5；Claude Code 2.x / claude-…。
  版本号跑 `codex --version` 或 `claude --version` 能查到）
- 插件装在哪、装了哪些部分：（路径；看一眼技能目录，skills 全装还是只有几个；
  agents/ 有没有装）
- 项目引擎：（Godot / Unreal / Unity / Web / 其他）

## 1. 本会话做了什么

一句话任务。用到的 skill / agent 清单，每个标：用户点名 / 你自己判断触发 /
用户贴了文件让你读。

## 2. 触发对错

- 该用却没用上的 skill：哪一个，为什么（没看到描述 / 描述没命中 / harness 没这个机制）
- 用上了但事后看不该用的：哪一个，为什么

## 3. 执行不了的指令

插件是给 Claude Code 写的，引用了一些 Claude Code 专属机制。逐条列出你遇到的、
在你的 harness 里无法直接执行的指令：原文 + 路径 + 你实际怎么办的
（跳过 / 换了等价做法 / 停下问用户）。

已知的专属机制供对照：Skill 工具、Agent / Task 子代理（含 Explore、`run_in_background`）、
AskUserQuestion、SendMessage / TeamCreate / TaskUpdate（Teams）、Workflow 编排、
`game-toolkit:xxx` 这种带前缀的引用、`agents/` 目录。

## 4. 脚本与工具

跑过的插件脚本（如 `check_config.py`、生成脚本、`project_env.py`）：命令、退出码、
报错原文。跑通的也列一行。

## 5. 读了但没照做的指令

原文 + 路径 + 原因（和项目冲突 / 看不懂 / 觉得多余 / 和另一条矛盾）。

## 6. 靠猜的地方

指引没说清、你自己定了的事：猜了什么、依据是什么、猜错会有什么后果。

## 7. 指引之间的矛盾

两处说法对不上的：两边原文 + 路径。插件文件之间的算，插件文件和它自己的脚本、
脚本注释之间的也算。

## 8. 最想改的一件事

一句话。
```
