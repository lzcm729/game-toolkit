用中文回答。

你是一个装载了游戏开发工具插件的 AI 助手，正在帮用户建这个仓库的 `asset-config.yaml`。
上一轮你（或另一位助手）读完了项目，把草稿和要问用户的问题写在了工作目录下的 `QUESTIONS.md`。
请先读它。工作指引仍是：

    {skill}

字段说明：{readme}
校验脚本：{check}
生成器（dry-run 不调 API，可以跑）：{generate}
Python：{python}

用户看完 `QUESTIONS.md`，回答如下（按事情说的，不按题号）：

- **范围**：就是道具表里的四条道具，全部都要。
- **用途**：背包格用，实际显示 64×64；视角三分之四略俯视；背景要真实透明底。
- **风格**：先探索新方向。那张风格锚是占位的，别用。稀有度不画进图标，UI 那边管。
- **输入能力**：先纯文字生成，不用风格参考、也不编辑底图。
- **后端**：laozhang。
- **模型**：gemini-3-pro-image，要质量好的。
- **比例**：1:1。
- **输出位置**：ArtSource/Props，导入脚本从那里读。
- **版本控制**：正式源图入 Git。
- 上面没提到的，用户没有别的意见。

请继续：按指引把 `asset-config.yaml` 写出来，跑校验，核对实际生效的参数与完整 prompt，然后停下。

边界：不能生成图片（没有网络和密钥），不要 `git add` / `git commit`。

**完整的最终报告放在最终回答里**：配置全文、校验的完整输出和退码、dry-run 里核对了什么，
以及你认为还没完成、需要用户接着做的事。如果指引里有哪句话让你拿不准、或者你觉得指引和
校验器说的不一致，也请指出来 —— **这一项最有价值**。
