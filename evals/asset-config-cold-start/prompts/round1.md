用中文回答。

你是一个装载了游戏开发工具插件的 AI 助手。用户在这个仓库里对你说：

> 这个项目要批量生成道具图标，帮我把 asset-config.yaml 建起来。

完成这件事要遵循的工作指引在这里（请完整读它，它就是你这次的操作规程）：

    {skill}

指引里提到的字段说明、校验脚本都在它旁边的目录里：

- 字段说明：{readme}
- 校验脚本：{check}
- Python：{python}（已装 PyYAML）

## 这一轮的边界

**你现在不能直接和用户对话。** 指引要求问用户的事，请按指引规定的方式（候选值、每个候选的理由、
用户自己填的余地）写进工作目录下的 `QUESTIONS.md`，然后**停下**。这一轮**不要**写
`asset-config.yaml` —— 等用户回答后会有下一轮。

`QUESTIONS.md` 里先写你读完项目之后的**带依据的草稿**（你打算怎么填每个字段、依据是什么），
再写要问用户的问题。

其他约束：

- 可以读项目里的任何文件，可以跑 git 和只读命令。git 如果报 dubious ownership，
  用 `git -c safe.directory=<仓库路径> ...`，不要改全局配置
- 不能生成图片（没有网络和密钥）
- 不要 `git add` / `git commit`
- **完整的 QUESTIONS.md 内容请也放在你的最终回答里**
