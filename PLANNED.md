# 下一版计划（未发布）

**这不是版本记录。** 标题刻意不用 `## x.y.z` 格式——`scripts/verify_release.py` 用
`^##\s+(\d+\.\d+\.\d+)\s` 找 CHANGELOG 的最新段，一个带版本号的计划标题会被它当成真的版本段，
造成假通过。已发布的内容一律进 `CHANGELOG.md`，这里只放还没做的。

发版后把对应条目从本文件删掉，正文写进 CHANGELOG。

---

## 冷启动评测挖出来、还没修的

4.5.0 把冷启动评测放进了仓库（`evals/asset-config-cold-start/`）。它正式上线的第一次运行，
codex 在第二轮回答的最后一节指出了 5 处指引与实现的出入，核实后 3 处属实：

1. **字段名拼错，check 照样退 0。**实测把 `aspect_ratio` 拼成 `aspect_ration: "4:3"`、
   再加一个不存在的 `quality: high` —— 校验通过，一个字不提。比例拼错，生成时静默退回
   默认值。计划层只取已知字段，没人看剩下的。该在四类已知位置（顶层 / `style` /
   `categories.*` / `categories.*.data_source`）上报「不认识的字段」，最好附上最像的
   那个已知字段名。**这条是功能缺口**，另两条是文档。

2. **`asset-config/SKILL.md` 说「所有相对路径都以工程根为基准」不准确。**裸相对的
   `reference_paths` / `image` 以 `output_root` 为基准；`project_root` 自己的相对值以
   config 所在目录为基准。

3. **文档说 check 的退码只有 0 / 1 / 3。**依赖缺失（PyYAML、generate-assets 的脚本）时
   还会退 2（`check_config.py` 两处 `SystemExit(2)`）。

另外两处不属实或已处理：透明底与画质没有接口保证 —— 4.4.0 已写进指引；「完成条件是
小样」与「本轮禁止生成」要分开记 —— 它自己处理对了，不是指引的问题。

---

## 留到下一个大版本一起破坏的（5.0.0）

三件都已经在 4.x 里**先告警**，给用户留了迁移窗口。5.0.0 把告警翻成报错 /
移除。列在一处，免得到时候发成三个大版本。

1. **`adapter: unreal` 移除。** 3.17.0 起它是兼容值、等价于 `filesystem`，每次都打
   一条提示说明等价关系。提示本身是成本，不该永远背着。
   代码位置：`engine_adapter.py` 的 `LEGACY_ADAPTERS` 与 `_LEGACY_WHY`。

2. **`adapter: generic` 移除。** 4.2.0 改名为 `filesystem`，旧名是兼容值。
   同一处代码。和第 1 条一起删掉之后 `LEGACY_ADAPTERS` 就空了，
   `_canonical()` / `legacy_note()` 连同那张表可以整个拿掉。

3. **`adapter: filesystem` 下 `data_source.path` 写 `res://` 改成报错。** 4.1.0 起告警。
   以前 `data_source.py` 无条件剥 `res://`、不问适配器，而 `output_root` /
   `reference_paths` / `image` 都走适配器 —— 同一份配置里同一个前缀两套解释。
   它曾是文档写明「接受」的行为，所以没直接报错。
   代码位置：`asset_plan._route_data_source_path` 里 `raw.startswith("res://")`
   那个分支，改成 `plan.errors` 即可。

## 还没做的（3.7.0 发版后剩下的）

3.7.0 已落地「必须进」5 条与「建议进」3 条，正文见 `CHANGELOG.md` 的 3.7.0 段。
下面两件当时就判为**单独排版**，以及三件明确**不做**的决定，留在这里备查。

### 建议单独排版

9. **反向流程完整重构。** 结论：**保留 `sync-code-ahead` 独立入口，不并进正向五阶段。**
   两者对象不同——正向的稳定身份是要求键、增量依据是文档与代码依赖变化、完成条件是报告与综合
   完成；反向的稳定身份是首次入队后不变的 pending ID、增量依据是两个源码 tree 的差异＋全部旧
   pending 重核、完成条件是某项文档同步成功并核验后移出队列。反向并非没有基线机制：它分离
   `scan` 与 `pending`、固定 H/T、从 `git show <H>:<path>` 读源码、支持暂缓与 rebase/squash 后
   继续。硬搬覆盖率与「要求新增／消失」没有自然分母。该补的是候选证据复核、裁决路由、
   可验证的队列操作。
   （更正一处旧表述：3.6.0 已加 Code-only→pending 接收入口，所以不是「两版没动」，
   准确说法是「完整结构重构两版未做」。）
10. **完整的状态声明审计。** 代码事实核验归 `sync-docs-ahead`，文档之间的声明冲突仍归
    `doc-consistency-check`，不扩大后者的源码职责。注意：**不是「首次让工具检查状态声明」**——
    09-09 报告已经核过拽尾救援的「已接通」并判 Missing；准确的需求是「保证**所有**实现状态声明
    都被枚举和核验」。设计时先区分「设计关系已接通／静态调用链已接通／资产已配置／运行验证通过」，
    再记录声明对象、范围、时间或版本、证据。CatFishing 那张接线表甚至没有专门状态列，
    「已接通」写在备注里，所以不能只靠规定列名覆盖。

### 不做（决定记录）

- **不为旧基线凭空补 `Code-only mechanics`**：缺的是当时证据，当前代码还原不了「当时是否查过」。
  三轮有该节的系统数 0／2／11，脚本已明确显示「无法确认」、没把未知当零，保留这个诚实边界。
- **不做键写回远程设计文档**：理由是收益不足，不是「远程来源已消失」。正文 SSOT 已在仓库、
  内容 CSV 仍在飞书填写后拉入；现有 token 身份与工程侧键已能连接这些来源。
- **不做 Unity 验证工程**：没有证据表明它比两套真实工程已暴露的问题更值得占版本范围。

### 一条流程教训（3.6.1 踩的）

`verify_release.py` **不查测试**。改了哪个 skill 就要跑那个 skill 的套件——3.6.1 只跑了
`generate-assets`，把自己改的 `sync-docs-ahead` 那条红用例发了出去。
