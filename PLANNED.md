# 下一版计划（未发布）

**这不是版本记录。** 标题刻意不用 `## x.y.z` 格式——`scripts/verify_release.py` 用
`^##\s+(\d+\.\d+\.\d+)\s` 找 CHANGELOG 的最新段，一个带版本号的计划标题会被它当成真的版本段，
造成假通过。已发布的内容一律进 `CHANGELOG.md`，这里只放还没做的。

发版后把对应条目从本文件删掉，正文写进 CHANGELOG。

---

## 3.7.0 范围（2026-09-13 定）

来源：codex 只读评审（工作根为本开发主目录，另读 CatFishing 三轮共 33 份真实产物），
Claude 逐条抽验。**它驳回了 Claude 原来的排序**，理由不是「都重要」，而是正向流程已有
产物证明结论会错，而「反向流程文件数少」不能证明它更危险。

抽验记录（Claude 复现，五条全中）：

| 主张 | 复现结果 |
|---|---|
| 09-11 有 44/709 行是 `unknown#L0#…` | 命中，分布也对：环境 34／道具 9／联机 1 |
| 三轮有 `Code-only mechanics` 节的系统数 0／2／11 | 命中 |
| 商店迁移表把同一机制同时列为新增与消失 | 命中：`TRANSITIONS.md:152` 新增、`:163` 消失，同一个摊位距离证明 |
| `--plan` 信 manifest 不核文件内容 | 命中，且比报告更糟：CatFishing 27 份设计文档有 10 份内容哈希已过期（已在该项目侧刷新并加了 `Scripts/refresh_design_manifest_hashes.py`） |
| Claude 用「`TailPull` 零命中」当依据是错的 | codex 对：工程名是 `TailRescue`，枚举与空壳结构都在；结论仍成立但依据已换 |

### 必须进

1. **增量计划的输入失效判断。** `document_revisions()`（`collect_gap.py:526`）只读 manifest 的
   版本字段、不核文件内容，`make_plan()`（`:575`）据此判断是否重跑，于是一份没更新的 manifest
   能替改过的正文证明「未变」，整份报告被沿用。改：记录并比较**实际输入文件指纹**，manifest 的
   token 继续只负责身份；把裁决账本与参考文件纳入失效依赖，旧 RUN 缺这些记录时保守重跑。
   同类问题在代码依赖侧：09-11 联机报告实际补扫了 `Fishing/Integration`、`Save`、`UI/Frontend`，
   而配置的联机 `code` 没有这些目录——**源码线索可以不完整，但用来证明「无需重跑」的依赖范围
   必须覆盖实际证据**，无法确认时扩大范围重跑。
2. **综合阶段加「属主依据一致才能派活」。** 已经出现把文档状态错误转换成实现任务的产物：
   09-09 报告指出拽尾救援「已接通」与代码不符，综合清单给出的动作却是「补两个 BodyAction」；
   09-11 联机报告判它 Missing，同轮多人报告把 `TailRescue` 列为「已裁不做、可删」。
   同一机制出现「补实现／删除／属主不承诺」三种结论时，综合者先核属主规格与现行裁决，
   冲突未解决不得进入「设计已定代码未跟」；也不能从「未实现」反推「已裁定删除」。
   `synthesis-prompt.md:48` 已有「未定需求不能伪装成开发任务」的规则，这一关不必等状态列 schema。
3. **收集器的语义校验与「复核改判」计数。** 两处都有数量证据：
   - 出处未解析仍成功铸键：09-11 有 44/709 行是 `unknown#L0#…`。环境报告写
     `环境与氛围.md rev106:65`，后续多行只写 `:65`；道具报告写 `第2-6行`；
     `source_ref()`（`:222`）的正则只认紧随扩展名的行号或单个 `第N行`。
     改：支持明确的行范围与版本括注；`inspect_report()` 对**本轮**报告要求出处可解析，
     不再省掉文件名还静默通过。旧报告兼容读取但明确警告。
   - 「复核改判」混计：09-11 的 32 实际是 **16 次五状态之间变化 ＋ 14 条要求补行 ＋ 2 条其他**
     （脚本 `:155` 只比较两格字符串是否相等）。改：`inspect_report()` 区分三类，
     `summary_text()` 分别显示。
   - `Code-only mechanics` 同理：模板要求必有，`:91` 的必选标题里没有它；
     对本轮新报告补检查，保留旧基线缺节的兼容。
4. **同文档、同 h8 的锚点漂移匹配。** 真实影响比 CHANGELOG 记的稍多：09-09→当日下午有
   鱼的「不可食用」一对 `r13→r10`；当日下午→09-11 有三对 UI CSV 漂移，另有一对商店 Markdown
   锚点 `3.1.2→2`——所以按「锚点漂移」实现，不限定 CSV。
   `mint_key()`（`:287`）不用改；改 `match_rows()`（`:388`），顺序为
   **完全同键 → 同 doc 同 h8 的唯一配对 → 同 doc 同 anchor 的相似度匹配 → 新增／消失**。
   机械接在现有模糊匹配之后会打架：模糊匹配会先耗掉本该配给漂移行的基线行。
   排除 `unknown` 身份；同 doc 同 h8 有多个候选时不盲配（h8 去掉了数字，重复要求消不了歧义）。
   `transitions_text()`（`:422`）要区分「锚点漂移」与「措辞改写」，不能套用当前「二级一律计改写」。
   旧 RUN **不用迁移**。
   **口径纠正**：稳定率定义是一级同键数÷基线行数，所以原来的 644/645＝99.8% 算得对；
   修好漂移匹配后这个数字仍应是 99.8%，漂移找回的行另列，不能冒充一级匹配。
5. **停止把 Code-only 描述文字差异直接宣布为机制新增／消失。** 09-11 商店迁移表把距离证明、
   两段式交付、分类页同时列为新增与消失，两轮「距离证明」还指向同一个
   `CatShopKioskActor.cpp:87-102`；脚本（`:463`）只存机制描述字符串再做集合差。
   本版的有限修法：降级为「描述差异、待核机制变化」，交复核／综合确认并保留 code_ref。
   完整机制身份体系另版。否则给旧基线补齐节只会得到更多假新增／消失。

### 建议进

6. **分析者／复核者返回前自跑校验。** 需要一个可用于**单报告、区分阶段**的检查入口，不能只在
   提示词加一句命令：当前入口（`:91`、`:691`）接目录且无条件要求复核记录，而分析者刚写完时
   还没有那一节、同目录别的系统可能正在写。主流程最后仍做全系统 `--expect` 汇总。
7. **`sync-code-ahead` 补裁决与候选复核边界。** 阶段 3 后补候选复核：核源码行为、属主设计、
   现行裁决与目标文档，明确哪些获准补文档、哪些仍待设计判断、哪些应回到代码修正。
   现结尾只有「与设计意图有差异，在文档注明基于代码同步」（`SKILL.md:326`），
   力度弱于正向的「不因代码已存在就修改需求」。
8. **补 `--carry` 示例的 `--project-root`。** 3.6.1 的接线遗漏：脚本已接收工程根，
   `SKILL.md:59` 与 `examples/README.md:70` 两个示例没传，照抄会触发绝对路径回退。

### 建议单独排版

9. **反向流程完整重构。** 结论：**保留 `sync-code-ahead` 独立入口，不并进正向五阶段。**
   两者对象不同——正向的稳定身份是要求键、增量依据是文档与代码依赖变化、完成条件是报告与综合
   完成；反向的稳定身份是首次入队后不变的 pending ID、增量依据是两个源码 tree 的差异＋全部旧
   pending 重核、完成条件是某项文档同步成功并核验后移出队列。反向并非没有基线机制：它分离
   `scan` 与 `pending`、固定 H/T、从 `git show <H>:<path>` 读源码、支持暂缓与 rebase/squash 后
   继续。硬搬覆盖率与「要求新增／消失」没有自然分母。该补的是候选证据复核、裁决路由、
   可验证的队列操作。
   （更正一处旧表述：3.6.0 已加 Code-only→pending 接收入口，所以不是「两版没动」，
   准确说法是「完整结构重构两版未做」。）
10. **完整的状态声明审计。** 代码事实核验归 `sync-docs-ahead`，文档之间的声明冲突仍归
    `doc-consistency-check`，不扩大后者的源码职责。注意：**不是「首次让工具检查状态声明」**——
    09-09 报告已经核过拽尾救援的「已接通」并判 Missing；准确的需求是「保证**所有**实现状态声明
    都被枚举和核验」。设计时先区分「设计关系已接通／静态调用链已接通／资产已配置／运行验证通过」，
    再记录声明对象、范围、时间或版本、证据。CatFishing 那张接线表甚至没有专门状态列，
    「已接通」写在备注里，所以不能只靠规定列名覆盖。

### 不做

11. **不为旧基线凭空补 `Code-only mechanics`。** 缺的是当时证据，当前代码还原不了「当时是否
    查过」。三轮有该节的系统数 0／2／11，脚本（`:458`）已明确显示「无法确认」、没把未知当零，
    保留这个诚实边界。该修的是新报告漏节仍通过，以及现有假变化。
12. **不做键写回远程设计文档。** 理由是收益不足，不是「远程来源已消失」：正文 SSOT 已在仓库，
    内容 CSV 仍在飞书填写后拉入；现有 token 身份与工程侧键已能连接这些来源，写回远程不能修复
    本轮查出的匹配与校验问题。
13. **不做 Unity 验证工程。** 没有证据表明它比两套真实工程已暴露的问题更值得占本版范围。

### codex 查过、认为目前没问题的部分

五状态计数与 Inspectable／Coverage 的数学实现（09-11 的 709、96.1%、49.3% 可重算）；
完全同键优先与「基线行只用一次」的不变量；manifest 三类条目与历史快照 manifest 优先级
（3.6.0 的两处修复仍在）；3.6.1 的路径转换方向与旧 RUN 兼容；反向检查点的核心恢复约定。
